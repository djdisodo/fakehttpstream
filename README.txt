made in attempt to cast screen to old tv that only supports dlna
not really functional

takes video file from stdin(as stream) and has internal buffer so it can rewind a bit

poorly written http server
